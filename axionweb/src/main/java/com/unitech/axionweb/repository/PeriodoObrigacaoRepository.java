package com.unitech.axionweb.repository;

import com.unitech.axionweb.model.PeriodoObrigacao;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PeriodoObrigacaoRepository extends JpaRepository<PeriodoObrigacao, Long> {
}
